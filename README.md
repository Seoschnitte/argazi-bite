# Клёво! Аргази

Telegram Mini App для отслеживания индекса клёва на Аргазинском водохранилище.

## Описание проекта

Это приложение позволяет рыбакам делиться оценками активности рыбы на Аргазинском водохранилище в режиме реального времени. Приложение собирает данные от реальных рыболовов и показывает актуальный индекс клёва для разных видов рыб.

### Основные функции

- Просмотр текущей погоды и общего индекса клёва
- Оценка клёва по 5 видам рыб: Судак, Щука, Окунь, Лещ, Плотва
- Статистика за разные периоды: Сегодня, Неделя, Месяц, Год
- Геолокационная проверка (можно оценивать только находясь на водохранилище)
- Ограничение частоты оценок (6 часов между оценками для каждого вида рыбы)

## Технологический стек

- **Frontend:** React + TypeScript + Vite + Tailwind CSS
- **Backend:** Supabase (PostgreSQL + Edge Functions)
- **Mini App SDK:** @twa-dev/sdk
- **Weather API:** OpenWeatherMap
- **Icons:** Lucide React

## Предварительные требования

Перед началом установки убедитесь, что у вас есть:

1. Аккаунт на [GitHub](https://github.com)
2. Аккаунт на [Supabase](https://supabase.com)
3. API ключ от [OpenWeatherMap](https://openweathermap.org/api)
4. Telegram бот (создается через [@BotFather](https://t.me/botfather))

## Установка и настройка

### Шаг 1: Клонирование репозитория

```bash
git clone <your-repo-url>
cd argazi-fishing-app
npm install
```

### Шаг 2: Настройка Supabase

1. Создайте новый проект на [Supabase](https://supabase.com)
2. Перейдите в раздел "SQL Editor"
3. База данных и миграции уже созданы автоматически
4. Скопируйте URL проекта и ANON KEY из "Settings" → "API"

### Шаг 3: Получение API ключа OpenWeatherMap

1. Зарегистрируйтесь на [OpenWeatherMap](https://openweathermap.org/api)
2. Получите бесплатный API ключ
3. Активация может занять несколько часов

### Шаг 4: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_WEATHER_API_KEY=your-openweathermap-api-key
```

### Шаг 5: Edge Function для погоды

Edge функция уже развернута. Убедитесь, что переменная окружения WEATHER_API_KEY настроена в Supabase:

1. Перейдите в Supabase → "Edge Functions" → "Secrets"
2. Добавьте секрет `WEATHER_API_KEY` с вашим OpenWeatherMap API ключом

## Развертывание на GitHub Pages

### Шаг 1: Настройка Vite для GitHub Pages

Откройте `vite.config.ts` и добавьте базовый путь:

```typescript
export default defineConfig({
  base: '/your-repo-name/',
  plugins: [react()],
  optimizeDeps: {
    exclude: ['lucide-react'],
  },
});
```

### Шаг 2: Сборка проекта

```bash
npm run build
```

### Шаг 3: Развертывание на GitHub Pages

#### Вариант 1: Через GitHub Actions (рекомендуется)

Создайте файл `.github/workflows/deploy.yml`:

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_ANON_KEY: ${{ secrets.VITE_SUPABASE_ANON_KEY }}
          VITE_WEATHER_API_KEY: ${{ secrets.VITE_WEATHER_API_KEY }}
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
```

Затем добавьте секреты в GitHub:
1. Перейдите в "Settings" → "Secrets and variables" → "Actions"
2. Добавьте все три переменные окружения

#### Вариант 2: Ручное развертывание

```bash
npm install -g gh-pages
npm run build
gh-pages -d dist
```

### Шаг 4: Настройка GitHub Pages

1. Перейдите в репозиторий → "Settings" → "Pages"
2. В разделе "Source" выберите "gh-pages" branch
3. Нажмите "Save"
4. Ваше приложение будет доступно по адресу: `https://your-username.github.io/your-repo-name/`

## Настройка Telegram Mini App

### Шаг 1: Создание бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните токен бота

### Шаг 2: Создание Mini App

1. Отправьте боту команду `/newapp`
2. Выберите вашего бота
3. Введите название: "Клёво! Аргази"
4. Добавьте описание: "Индекс клёва на Аргазинском водохранилище"
5. Загрузите иконку приложения (512x512 px)
6. Вставьте URL вашего GitHub Pages: `https://your-username.github.io/your-repo-name/`
7. Загрузите скриншоты (опционально)

### Шаг 3: Запуск приложения

Теперь ваше Mini App доступно через вашего Telegram бота!

## Структура проекта

```
project/
├── src/
│   ├── components/
│   │   ├── FishCard.tsx         # Карточка рыбы с индексом клёва
│   │   ├── MainScreen.tsx       # Главный экран приложения
│   │   ├── RatingForm.tsx       # Форма оценки клёва
│   │   ├── Tabs.tsx             # Компонент переключения периодов
│   │   └── WeatherWidget.tsx    # Виджет погоды
│   ├── lib/
│   │   └── supabase.ts          # Конфигурация Supabase клиента
│   ├── utils/
│   │   └── helpers.ts           # Вспомогательные функции
│   ├── types.ts                 # TypeScript типы
│   ├── App.tsx                  # Главный компонент с Telegram SDK
│   └── main.tsx                 # Точка входа
├── supabase/
│   └── functions/
│       └── get-weather/         # Edge Function для получения погоды
└── .env.example                 # Пример переменных окружения
```

## База данных

### Таблицы

**fish_types** - Типы рыб
- id (uuid)
- name_ru (text) - Название на русском
- name_en (text) - Название на английском
- icon_name (text) - Название иконки
- display_order (integer) - Порядок отображения

**ratings** - Оценки клёва
- id (uuid)
- telegram_user_id (text) - ID пользователя Telegram
- fish_type_id (uuid) - Ссылка на тип рыбы
- rating (numeric) - Оценка от 1.0 до 5.0
- latitude (numeric) - Широта
- longitude (numeric) - Долгота
- air_temp (numeric) - Температура воздуха
- pressure (numeric) - Атмосферное давление
- wind_speed (numeric) - Скорость ветра
- wind_direction (text) - Направление ветра
- created_at (timestamptz) - Время создания

### RLS политики

- Публичное чтение для всех таблиц
- Вставка оценок только для аутентифицированных пользователей

## Разработка

### Локальный запуск

```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Проверка типов

```bash
npm run typecheck
```

### Линтинг

```bash
npm run lint
```

### Сборка для продакшена

```bash
npm run build
```

## Тестирование геолокации

Приложение проверяет, находится ли пользователь в пределах Аргазинского водохранилища или не далее 1 км от его береговой линии.

Координаты границ водохранилища:
- A (Карасёво): 55.480871, 60.302051
- B (Кораблик): 55.350694, 60.303424
- C (Южный Горняк): 55.292394, 60.355123
- D (Байрамгулово): 55.370411, 60.505119
- E (Байк): 55.488239, 60.407015

## Устранение неполадок

### Проблема: Приложение не загружается в Telegram

**Решение:**
- Убедитесь, что URL в BotFather совпадает с фактическим URL GitHub Pages
- Проверьте, что приложение доступно по HTTPS
- Очистите кэш Telegram

### Проблема: Не работает геолокация

**Решение:**
- Убедитесь, что разрешили доступ к геолокации в браузере/Telegram
- Проверьте настройки приватности на устройстве
- Используйте HTTPS (GitHub Pages автоматически использует HTTPS)

### Проблема: Не загружается погода

**Решение:**
- Проверьте правильность API ключа OpenWeatherMap
- Убедитесь, что ключ активирован (может занять до 2 часов)
- Проверьте секреты в Supabase Edge Functions

### Проблема: Ошибка при отправке оценки

**Решение:**
- Проверьте подключение к интернету
- Убедитесь, что переменные окружения настроены правильно
- Проверьте консоль браузера для детальных ошибок

## Цветовая шкала индекса клёва

- 🔴 1.0-2.0: Красный (Не клюёт)
- 🟠 2.1-3.0: Оранжевый (Клёв слабый)
- 🟡 3.1-4.0: Жёлтый (Клёв средний)
- 🟢 4.1-5.0: Зелёный (Клюёт!)

## Ограничения

- Оценки можно ставить только находясь на водохранилище или в радиусе 1 км
- Между оценками одного вида рыбы должно пройти минимум 6 часов
- Требуется включенная геолокация

## Лицензия

MIT

## Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в GitHub репозитории.
